# مشروع مترجم لغة الإشارة
### ترجمة لحظية من كاميرا الويب إلى نصٍّ وصوت — بالعربية (ArASL) والإنجليزية (ASL)

---

## 1) فكرة المشروع

أداة سطح المكتب تجعل كاميرا الويب **مترجماً حيّاً للغة الإشارة**: تُمسك بكف اليد أمام الكاميرا، تُعرَف الإشارة حرفاً بحرف، ثم تُجمع الحروف في كلمة تُعرض على الشاشة وتُنطق صوتياً — مع تصحيح آلي للنص عبر ذكاء اصطناعي (Groq).

- **عربي**: 32 إشارة أحادية من قاعدة **ArASL2018** (ألف، باء، تاء، ثاء، …).
- **إنجليزي**: 24 حرفاً من **Sign Language MNIST** (A–Y، باستثناء J وZ) — كوضع منفصل تماماً.

مثال للاستخدام: يوقّع المستخدم «س → ل → ا → م»، فتعرض الشاشة «سلم» ثم تُصحَّح إلى «سَلِمَ» مع نطقها.

---

## 2) كيف يعمل (نظرة عامة)

```
        ┌──────────────┐
        │ كاميرا الويب  │   إطار BGR كل ~0.1ث
        └──────┬───────┘
               ▼
        ┌──────────────────────────────┐
        │ MediaPipe HandLandmarker     │  21 نقطة (معصم، مفاصل، أطراف الأصابع)
        │ (Tasks API · VIDEO mode)     │  ← يكشف/يتتبّع كفّ اليد
        └──────┬───────────────────────┘
               ▼
        ┌──────────────────────────────┐
        │ crop_hand_patch              │  قصّ مربع حول اليد → رمادي 64×64 في [0,1]
        │ + تطبيع القطبية              │  (خلفية سوداء ← يد بيضاء، مطابقة للتدريب)
        └──────┬───────────────────────┘
               ▼
        ┌──────────────────────────────┐
        │ CNN مصنّف                     │  softmax → top-1 + الثقة + الهامش (top1−top2)
        ├──────────────────────────────┤
        │ عربي: تحقّق هندسي SIGNPAT    │  4 مراسي مؤكّدة (ف/س/ث/ي): فرق أصابع ≥2 = رفض
        │ إنجليزي: margin-check ≥0.05  │  ← «غير مفهوم» بدل حرفٍ مخطئ
        └──────┬───────────────────────┘
               ▼
        ┌──────────────────────────────┐
        │ SignSequencer (debounce)     │  N إطارات متتالية ثابتة بثقة ≥0.90
        │ ثقة + صمت 2.5ث               │  → التزام حرف → كلمة → إقفال الكلمة
        └──────┬───────────────────────┘
               ▼
        ┌──────────────────────────────┐
        │ Groq correct_word (اختياري)  │  تصحيح الهجاء → «سلم» ← «سَلِمَ»
        │ + gTTS → mp3                 │  نطق تلقائي عبر st.audio
        └──────────────────────────────┘
```

الواجهة (Streamlit) موجّهة باللغة والدواة المبدّلة بين «عربي / English»، بتبويبين: **ترجمة لحظية** و**قاموس الإشارات**.

---

## 3) القرارات التقنية وأسبابها

| القرار | السبب |
|---|---|
| **MediaPipe Tasks API وليس `solutions.hands`** | `mediapipe.solutions` أُزيل نهائياً في الإصدار 1.0.1؛ Tasks API هو المسار الرسمي المدعوم، ويوفر وضع VIDEO مع timestamp لاستقرار التتبّع. |
| **تصنيف بالشبكة العصبية CNN على الصورة لا بميزات landmarks** | الداتا (ArASL / ASL-MNIST) صور رمادية ملوّنة باليد؛ CNN تستهلك هذا الفورمات مباشرة، وفازت بدقة عالية (96–100% في المختبر). ميزات الـlandmarks حساسة لمكان اليد والإضاءة ولا يوجد لها داتا تدريب جاهزة. |
| **طبقة تحقق هندسي (SIGNPAT) فوق الـCNN** | CNN قد تخطئ بأوضاع يد متشابهة؛ طبقة هندسية من 21 نقطة ترفض التناقض الصريح (مثل صنف «سين» بكف 4 أصابع بينما اليد قبضة) → تُعرض «غير مقروء» بدل حرف مخطئ. أربعة مراسي فقط مُؤكدة من صور مرجعية حقيقية — بلا افتراضات إضافية. |
| **إنجليزي منفصل تماماً عن العربي** | قاعدة ملزمة: لا خطر على خط الأنابيب العربي الشغّال. كل لغة بنموذجها/بياناتها/عتباتها المستقلة (`cnn_en.pt`, `asl_mnist.npz`, عتبات `*_EN`). التحقق الإنجليزي: **SIGNPAT_EN** بتجميعات عدد الأصابع الكنسية (18 حرفاً، تسامح ±1) + margin-check 0.05؛ المراسي الحرف-بحرف الصارمة مؤجلة لما بعد المعاينة الحية. |
| **تدرّج الكاميرا + مشفّر JPEG** | يُقاس لكل مرحلة (capture/detect/crop/classify/geometry/تشفير): الكشف شبه مستقل عن الدقة (27→33ms عبر 640→1920) بينما تشفير PNG ينفجر بالدقة (1080p: 25.6ms/53KB مقابل JPEG80 8.2ms/38KB). الإصلاح الثنائي: `downscale_live` (عرض أقصى 640) قبل المعالجة + `st.image(..., output_format="JPEG")`. النتيجة: **LivePipeline.update ≈ 28ms وسطياً / p90≈31ms / صفر أخطاء** (سابقاً كانت الحلقة تتجمّد لنقصان timestamp للكاشف عند تبديل اللغة). مفصّل في PROJECT_MAP §Polish9. |
| **PyTorch CPU بدل TensorFlow** | أخف وزناً وتحكماً، تعمل على أي جهاز بلا GPU؛ دقة متكافئة لمهمة أحادية القناة 32/24 فئة. |
| **Groq عبر REST مباشر بلا SDK** | توجه خفيف ومتحكم، استقر على موديل **`allam-2-7b`** العربي-المختص (بعد إزالة `llama-3.3-70b-versatile` من كتالوج Groq 2026). فشل الشبكة/المفتاح → fallback للنص الخام بلا انقطاع. |
| **gTTS للنطق** | صفر إعداد، جودة صوت مقبولة للعربي والإنجليزي؛ فشله لا يكسر التطبيق (يُعرض النص فقط). |
| **`st.fragment(run_every=0.1)` في Streamlit** | حلقة تشغيلية دقيقة واحدة/ثانية بلا إعادة رسم كاملة للصفحة (صديقة للأداء في اللقطات الثلاث). |

---

## 4) النتائج

| المعامل | العربية (ArASL) | الإنجليزية (ASL) |
|---|---|---|
| عدد الصور | 54,049 | 34,627 (تدريب 27,455 + اختبار 7,172) |
| عدد الفئات | 32 إشارة | 24 حرفاً |
| بنية الـCNN | 1×64×64 → Conv/BatchNorm → Linear(4096→32) | نفس المعمارية، رأس 24 فقط |
| دقة التحقق (val، قسمة 85/15) | **95.18%** (epoch 3 من 3) | **99.98%** (آخر تدريب؛ أفضل ملاحظة 100.0%) |
| عتبة الالتزام | ثقة ≥ 0.90 + 5 إطارات | ثقة ≥ 0.90 + هامش ≥ 0.05 + 5 إطارات |
| أضعف 3 أصناف | قاف 81.6%، ثاء 89.4%، زاي 90.8% | لا صنف ضعيف ملحوظ |

> **تنبيه علمي صادق:** دقة «val» تُمثّل الأداء المعملي على تقسيمة عشوائية من الداتا نفسها، وليست ضماناً للاستخدام الحي؛ الأداء الفعلي على الكاميرا يتأثر بالإضاءة، وزن اليد، والتوافق مع الصور المرسومة للتدريب. هذا النطاق بقي **بانتظار اختبار المعاينة الحية** (EN-6).

---

## 5) حدود معروفة

- أربعة مراسي هندسية عربية فقط (ف/س/ث/ي)؛ بقية الأصناف تعتمد الـCNN وحده. شكل «ألف/ياء» متقارب هندسياً فيُحسم بالثقة. الإنجليزي مؤطّر بتجميعات عدد الأصابع فقط (قياس MNIST يكشف انحيازاً منهجياً — B≈2 عمود/ A≈3/ W≈4 — فلا عنادل حرف-حرف قبل بيانات حية).
- HandLandmarker لا يلتقط صور ArASL **المرسومة** (ومثيلها الإنجليزي) ← لا يمكن جعل «مسح قياسي» شامل عبر الداتا؛ التحقق الهندسي للحروف الإنجليزية يُتمّ عملياً بالكاميرا الحية فقط.
- الحروف المجمّعة تُعرض ككلمة نصية؛ لا يوجد حتى الآن «قوالب إشارات» لكلمات كاملة بملامح واحدة (انظر §6).
- يتطلب إنترنت عند أول تنزيل داتا، ولتصحيح Groq/النطق.

---

## 6) تطوير مستقبلي (خارج نطاق هذا الإصدار)

- **قوالب إشارات لكلمات/جمل كاملة** (كلمة واحدة = إشارة واحدة) بدل تجميع حرف-بحرف.
- **Practice mode**: وضع تدريب يعرض إشارة ويثمّن أداء المستخدم (بمقياس هندسي أو CNN).
- **مراسي SIGNPAT إنجليزية حرف-بحرف** معتمدة من صور حيّة (اكتملت تجميعات الأصابع في `SIGNPAT_EN`؛ تبقى العتبات الحرفية الدقيقة وفحص `thumb` وكذلك فصل المتطابقات مثل D/I وH/U/R/K).
- تحسين الأصناف الضعيفة العربية (قاف/ثاء/زاي) ببيانات أكثر أو أوضاع إرجاز.
- تصدير النماذج إلى ONNX/TFLite وتقليل الوزن لسرعة أعلى على أجهزة منخفضة.

---

## 7) التشغيل

```
py -3.11 -m venv .venv
.\.venv\Scripts\pip install -r requirements.txt
.\.venv\Scripts\python.exe engine.py --fetch-data     # بيانات + صور القاموس (عربي)
.\.venv\Scripts\python.exe engine.py --train          # نموذج cnn.pt (val ≥95%)
.\.venv\Scripts\python.exe engine_en.py --fetch-en    # بيانات الإنجليزية (كاغل)
.\.venv\Scripts\python.exe engine_en.py --train-en    # نموذج cnn_en.pt
.\.venv\Scripts\python.exe engine.py --selftest       # 51 فحصاً شاملاً (عربي+إنجليزي)
.\.venv\Scripts\python.exe -m streamlit run app.py    # الواجهة
```

- يتوفر أي نموذج/داتا عبر الأوامر أعلاه (الأرتيfacts خارج git).
- مفتاح Groq في `.env` (اختياري — تصحيح النص فقط؛ النص الخام يعمل بدونه).