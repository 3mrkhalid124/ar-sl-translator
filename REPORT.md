# مشروع مترجم لغة الإشارة
### ترجمة لحظية من كاميرا الويب إلى نصٍّ وصوت — بالعربية (ArASL) والإنجليزية (ASL) + إشارات كلمات إنجليزية (ISLR)

---

## 1) فكرة المشروع

أداة سطح المكتب تجعل كاميرا الويب **مترجماً حيّاً للغة الإشارة**: تُمسك يدك أمام الكاميرا، تُعرَف الإشارة حرفاً بحرف، ثم تُجمع الحروف في كلمة تُعرض على الشاشة وتُنطق صوتياً — مع تصحيح آلي للنص عبر ذكاء اصطناعي (Groq). أربعة أنظمة مستقلة بالكامل، لكلٍّ بياناته ونموذجه وعتباته:

- **عربي (حروف)**: 32 إشارة أحادية من قاعدة **ArASL2018** (ألف، باء، تاء، ثاء، …).
- **إنجليزي (حروف)**: 24 حرفاً من **Sign Language MNIST** (A–Y، باستثناء J وZ).
- **إنجليزي (كلمات/ISLR)**: 10 كلمات إشارية كاملة (كلمة = إشارة واحدة) من **Kaggle asl-signs** — hello, thankyou, please, yes, no, bye, drink, water, happy, sleep.
- **واجهة Streamlit** تجمع الثلاثة في تبويبات مع قاموس مصوّر.

مثال للاستخدام: يوقّع المستخدم «س → ل → ا → م»، فتعرض الشاشة «سلم» ثم تُصحَّح إلى «سَلِمَ» مع نطقها؛ أو يوقّع «شُكراً» كإشارة واحدة كاملة فتُنطق جملة متراكمة بمسافات واضحة.

---

## 2) كيف يعمل (نظرة عامة)

```
        ┌──────────────┐
        │ كاميرا الويب  │   إطار BGR كل ~0.1ث
        └──────┬───────┘
               ▼
        ┌──────────────────────────────┐
        │ MediaPipe HandLandmarker     │ 21 نقطة (معصم، مفاصل، أطراف الأصابع)
        │ (Tasks API · VIDEO mode)     │ ← يكشف/يتتبّع كف اليد
        └──────┬───────────┬───────────┘
               ▼           ▼
   ┌─────────────────┐  ┌───────────────────────────────┐
   │ حروف (عربي/إن)   │  │ كلمات إنجليزية (ISLR)          │
   │ crop_hand_patch │  │ انعكاس نِسبي للمعصم (wrist)    │
   │ → رمادي 64×64   │  │ → نافذة 40 إطاراً (إعادة عينات) │
   │ + تطبيع قطبية   │  │ → LSTM 64/64/32+Dropout        │
   └────────┬────────┘  └───────────────┬───────────────┘
            ▼                          ▼
   ┌────────────────────────────────────────────────────┐
   │ CNN softmax → top1 + ثقة + هامش (top1−top2)         │
   │ عربي: تحقّق هندسي SIGNPAT (4 مراسي: ف/س/ث/ي)         │
   │ إنجليزي حروف: SIGNPAT_EN (تجميعات عدد الأصابع)       │
   └──────────────┬─────────────────────────────────────┘
                  ▼
   ┌────────────────────────────────────────────────────┐
   │ SignSequencer (debounce)                           │
   │ N إطارات ثابتة بثقة ≥0.90 → حرف → كلمة → إقفال      │
   │ زر ✓ تثبيت (فوري بلا تحقق) + زر ␣ كلمة جديدة        │
   │ + إقفال تلقائي بالصمت 2.5ث                         │
   └──────────────┬─────────────────────────────────────┘
                  ▼
   ┌────────────────────────────────────────────────────┐
   │ Groq correct_word (اختياري) — تصحيح الجملة كاملة    │
   │ + gTTS → mp3 — نطق الجملة بفاصل كلمات              │
   └────────────────────────────────────────────────────┘
```

الواجهة (Streamlit 1.63) موجّهة باللغة عبر مبدّل «عربي / English» (توجيه RTL/LTR خالص)، بثلاثة تبويبات: **ترجمة لحظية** و**قاموس الإشارات** و**كلمات إنجليزية** (التبويب الحي ISLR). كل مؤشر/زر بمفتاح فريد (لا DuplicateElementId).

---

## 3) القرارات التقنية وأسبابها

| القرار | السبب |
|---|---|
| **MediaPipe Tasks API وليس `solutions.hands`** | `mediapipe.solutions` أُزيل نهائياً في الإصدار 1.0.1؛ Tasks API هو المسار الرسمي المدعوم، ويوفر وضع VIDEO مع timestamp لاستقرار التتبّع. |
| **تصنيف حروف بالشبكة العصبية CNN على الصورة** | الداتا (ArASL / ASL-MNIST) صور رمادية ملوّنة باليد؛ CNN تستهلك هذا الفورمات مباشرة، وفازت بدقة عالية. ميزات الـlandmarks حساسة لمكان اليد والإضاءة. |
| **طبقة تحقق هندسي (SIGNPAT) فوق الـCNN** | CNN قد تخطئ بأوضاع يد متشابهة؛ طبقة هندسية من 21 نقطة ترفض التناقض الصريح (مثل «سين» بكف 4 أصابع فيما اليد قبضة) → تُعرض «غير مقروء» بدل حرف مخطئ. أربعة مراسي عربية مؤكدة من صور مرجعية؛ الإنجليزي 18 حرفاً بتجميعات عدد الأصابع الكنسية (±1 تسامح) + margin-check 0.05. |
| **كلمات إنجليزية بمعمارية LSTM على landmarks** | إشارة الكلمة حركة عبر الزمن لا صورة ثابتة؛ لذا يُعاد أخذ عينات التسلسل إلى 40 إطاراً وكل إطار 126-بعداً (يدان × 21 × 3) ويصنّفها LSTM (مع wrist-relative لتجريد موضع/حجم اليد)+ per-channel scaler. نظام رابع منفصل تماماً (`engine_words_en.py`) لا يلمس خطي الحروف. |
| **التقسيم عند الكلمات: بالمشارِك (participant-held-out)** | منع تسريب المشاركين نفسه بين تدريب وتحقق/اختبار — يُقاس اختبار على مشاركين لم يَرَهم التدريب (test acc 63.64% على 33 مقطعاً). |
| **إنجليزي منفصل تماماً عن العربي** | قاعدة ملزمة: لا خطر على خط الأنابيب العربي الشغّال؛ لكل لغة نموذجها/بياناتها/عتباتها المستقلة (`cnn_en.pt`, `asl_mnist.npz`, عتبات `*_EN`). |
| **تدرّج الكاميرا + مشفّر JPEG** | قِيس لكل مرحلة؛ الكشف شبه مستقل عن الدقة بينما تشفير PNG ينفجر بالدقة. الإصلاح: `downscale_live` (عرض ≤640) قبل المعالجة + `st.image(..., JPEG)`. النتيجة: **LivePipeline.update ≈ 28ms / p90≈31ms** (سابقاً كانت الحلقة تتجمّد عند تبديل اللغة). |
| **مفاتيح فريدة لكل `st.button`/`download_button`** | إصلاح حرج لـ StreamlitDuplicateElementId: كل زر بمفتاح حرفي فريد لكل لغة (`btn_ok_commit_{lang}`…) — يتحقق منه AppTest عبر جميع الأزرار باللغتين. |
| **زر «✓ تثبيت» يدوي** | التثبيت الفوري للحرف المعروض بلا انتظار debounce/أقل ثقة — يتجاوز الحاجز عند التباطؤ (يستخدمه المستخدم عند صعوبة الثبات). |
| **كلمات منفصلة تُعرض بمسافات** | لا يُسلسَل النص بلا فواصل: الكلمات تُجمع في `words[]` وتُعرض كروابط (chips) بمسافات ظاهرة + سطر نصي بمسافات؛ Groq يصحّح **الجملة كاملة** (ليست كلمات منعزلة) وgTTS ينطقها بفواصل كلمات. زر «␣ كلمة جديدة» يغلق الكلمة فوراً (يكمل آلية الصمت 2.5ث). |
| **PyTorch CPU للحروف / TensorFlow-Keras للكلمات** | Torch خفيف ودقيق لأحادية القناة 32/24 فئة؛ Keras لتسلسلات LSTM على landmarks. |
| **Groq عبر REST مباشر بلا SDK** | توجّه خفيف ومتحكم؛ موديل **allam-2-7b** العربي-المختص في كتالوج 2026. فشل الشبكة/المفتاح → fallback للنص الخام بلا انقطاع. |
| **gTTS للنطق** | صفر إعداد، جودة مقبولة بالعربية والإنجليزية؛ فشله لا يكسر التطبيق (يُعرض النص فقط). |
| **`st.fragment(run_every=0.1)` في Streamlit** | حلقة تشغيلية دقيقة كل 0.1ث بلا إعادة رسم كاملة للصفحة — حلقتان مستقلتان (الحروف الحي + الكلمات الحي). |

---

## 4) النتائج

### الحروف — العربية (ArASL)

| المعامل | القيمة |
|---|---|
| عدد الصور | 54,049 (تقسيم تحقق 85/15 بالضبط كما في النموذج) |
| الفئات | 32 إشارة |
| بنية الـCNN | 1×64×64 → Conv/BatchNorm → Linear(4096→32) |
| دقة التحقق (val) | **97.05%** (آخر جولة: 8 epochs مع targeted oversampling للفئات الضعيفة — السلسلة 0.9571→0.9705) |
| أضعف 3 أصناف بعد التحسين | قاف 89.8%، فاء 93.9%، ذال 94.1% (كانت سابقاً قاف 75.8%/فاء 90.8%/كاف 88.0%) |

### الحروف — الإنجليزية (ASL-MNIST)

| المعامل | القيمة |
|---|---|
| عدد الصور | 34,627 (تدريب 27,455 + اختبار 7,172) |
| الفئات | 24 حرفاً (A–W, Y) |
| دقة التحقق | **≈100%** (آخر تدريب ≥ 99.98%) |
| عتبة الالتزام | ثقة ≥ 0.90 + هامش ≥ 0.05 + 5 إطارات |

### الكلمات — الإنجليزية (ISLR)

| المعامل | القيمة |
|---|---|
| التسلسلات | 320 (10 كلمات × ~32، 21 مشاركاً) — التقسيم بالمشارك: تدريب 253 / تحقق 34 / **اختبار 33** |
| دقة الاختبار (held-out) | **63.64%** (21/33) — أضعف: `no` 0.00 (n=1)، أقوى: `drink`/`hello` 100% |
| التحسين | من raw-coords 39.4% → wrist-relative + per-channel scaler = **63.64%** |
| جولة تحسين إضافية (--train-v2) | 4 وصفات×seed على نفس التقسيم (سقف 20 دقيقة): أفضل اختبار 63.64% (تعادل) — **لا نموذج جديد يفوق عددياً → أُبقيت القديمة** |
| ملاحظة على البيانات | 297 ملف parquet أخرى على القرص هي إشارات مختلفة (dog/cat/food…) **وليست** كلمات — لا مادة إضافية محلية للكلمات العشر |

### نظام الفحص الشامل

| الفحص | النتيجة الطرفية |
|---|---|
| `engine.py --selftest` (عربي + إنجليزي حروف) | **ALL PASS** (90 فحصاً) |
| `engine_words_en.py --eval` | words_en.test PASS (acc 0.6364 ≥ 0.55) |
| `apptest_sl.py` (واجهة Streamlit) | PASS — 12 حالة، 0 استثناءات/0 Duplicates (باللغتين + كل الأزرار + مبدّل القاموس) |
| **`selftest_all.py`** (الأربعة معاً، بوابة صفر-تراجع) | **ALL PASS — zero regression** (خروج 0) |

---

## 5) حدود معروفة

- أربعة مراسي هندسية عربية فقط (ف/س/ث/ي)؛ بقية الأصناف تعتمد الـCNN وحده. شكل «ألف/ياء» متقارب هندسياً فيُحسم بالثقة.
- الإنجليزي الحروف مؤطّر بتجميعات عدد الأصابع فقط (قياس MNIST يكشف انحيازاً منهجياً: B≈2 عمود/ A≈3/ W≈4)؛ المراسي الحرف-بالحرف الصارمة مؤجلة لما بعد بيانات حية أكثر.
- كلمات ISLR: اختبار 63.64% على مشاركين جدد صعب (كلمات منفصلة تشبه بعضها: hello/please كلاهما كف مفتوح عند الوجه)؛ n لكل صنف صغير (1–6) فيشذّ المصنف. لا توجد بيانات محلية إضافية أصلية للكلمات العشر.
- HandLandmarker لا يلتقط صور ArASL/MNIST **المرسومة** ← لا «مسح قياسي» شامل عبر الداتا؛ التحقق الهندسي يتم عملياً بالكاميرا الحية.
- **تجميد عرضي نادر في `HandLandmarker.detect_for_video` (محدود معروف — قياس 06-09-2026):** نادراً ما يستهلك كشفٌ واحدٌ جزءاً من الثانية إلى **7–12 ثانية** (رصد: 7.95s عربي في نافذة 236 إطاراً، 12.03s إنجليزي في 90 إطاراً، ووضع ممتد 213ms/إطار لمرتين)، بينما إجمالي المحرك طبيعي تماماً في العادة (update p50≈21–31ms، p90≈30–54ms؛ copy 0.1 · crop 0.8 · classify 3.8 · detect p50≈21–31ms). حلقة حصرية >31,000 إطاراً (18,310 + 11,650 + غيرها) لم تُعِد إنتاج أي تحميل >1.2s ⇒ ظاهرة **عرضية غير قابلة لإعادة الإنتاج في السيطرة** (تُشبه تدخّل سائق/خلفية في النظام على هذا الجهاز)، والعطل داخل `detect_for_video` بعد نسخ `cvtColor`/`mp.Image` (كلاهما دون 0.5ms). الممارسات المرصودة: أي تجميد ينتهي ويستأنف طبيعياً؛ **خط الدفاع الأخير**: زر «✓ تثبيت» اليدوي يواصل الالتزام الفوري حتى لو تجمّد عرض المعاينة مؤقتاً. المراقبة اللوجية جاهزة: `ENGINE_TRACE=1` يطبع `[LIVE-VOTE]` لكل إطار (إثبات أن نافذة الأغلبية 5/7 مستدعاة من `update→feed`)، و`profile[ar]/[en]` يعرض متوسط ms/مرحلة كل 60 إطاراً. إن ظهر التجمد متكرراً بشكل ممنهج على جهاز المستخدم أخيراً، اقتراح التحقيق التالي: تبني `RunningMode.LIVE_STREAM` مع timestamp محلي، أو تفويض الكشف إلى خيط منفصل مع مهلة وسقوط إطارات.
- يتطلب إنترنت عند أول تنزيل داتا، ولتصحيح Groq/النطق.

---

## 6) تطوير مستقبلي (خارج نطاق هذا الإصدار)

- رفع دقة الكلمات بـISLR: بيانات أكثر (نفس الـ10 كلمات من تنزيل Kaggle مقصود)، أو تصنيفات فرعية geo على landmarks للفئات المتقاربة.
- **Practice mode**: وضع تدريب يعرض إشارة ويثمّن أداء المستخدم.
- مراسي SIGNPAT إنجليزية حرف-بحرف معتمدة من صور حية (فصل متطابقات مثل D/I وH/U/R/K).
- تحسين الأصناف العربية الضعيفة (قاف/ذال) ببيانات أكثر.
- تصدير النماذج إلى ONNX/TFLite وتقليل الوزن لسرعة أعلى.

---

## 7) التشغيل

```
py -3.11 -m venv .venv
.\.venv\Scripts\pip install -r requirements.txt
.\.venv\Scripts\python.exe engine.py --fetch-data       # بيانات + صور قاموس (عربي)
.\.venv\Scripts\python.exe engine.py --train --target 0.99   # cnn.pt (val 97.05%)
.\.venv\Scripts\python.exe engine_en.py --fetch-en      # داتا الإنجليزية (كاغل)
.\.venv\Scripts\python.exe engine_en.py --train-en      # cnn_en.pt
.\.venv\Scripts\python.exe engine_words_en.py --train   # words_en.keras (ISLR 10 كلمات)
.\.venv\Scripts\python.exe engine_words_en.py --train-v2 [دقائق]  # بحث وصفات بميزانية
.\.venv\Scripts\python.exe engine_words_en.py --eval    # test acc على held-out
.\.venv\Scripts\python.exe selftest_all.py              # فحص الأربعة معاً (0 = نجاح)
.\.venv\Scripts\python.exe -m streamlit run app.py      # الواجهة
```

- الأرتيفاكتات (`models/*`, `data/*`) خارج git وتُبني بالأوامر أعلاه.
- مفتاح Groq في `.env` (اختياري — تصحيح النص فقط؛ النص الخام يعمل بدونه). مفتاح Kaggle في `%USERPROFILE%\.kaggle` لتنزيل الداتا.